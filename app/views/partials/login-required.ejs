<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Necessário</title>
    <style>

        :root {
  --laranja: #f56d44;
  --fundo: #ffffff;
  --azul-base: #141c44;
  --letras: #141c44;
  --azulbase: #141c44;
  --azulsec: #77b6ea;
  --amarelo: #ffbc3e;
}

.tema-claro {
  --laranja: #ff5c2a;
  --fundo: #f5f5f5;
  --azul-base: #141c44;
  --azulbase: #141c44;
  --azulsec: #77b6ea;
  --amarelo: #ffbc3e;
  --letras: #141c44;
  transition: all 0.2s ease-in;
  font-family: 'Poppins', sans-serif;
}

.tema-escuro {
--azulbase: #141c44;

  --letras: #f5f5f5;
  --fundo: #030303;
  --fundo2: #111111;
  transition: all 0.2s ease-in;
  --azul-base: #ffffff;
}
        body {
            background: var(--fundo);
            font-family: 'Poppins', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .dev-container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .msgDev {
            color: var(--letras);
            text-align: center;
            margin: 0 0 24px 0;
            font-weight: 600;
            font-size: 1.3rem;
        }
        .dev-img {
            width: 30%;
            max-width: 90vw;
            margin: 0 auto;
            display: block;
        }
        .btn-container {
            display: flex;
            gap: 20px;
            margin-top: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }
        .btn-fofo {
            background: #f56d44;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 14px 14px;
            font-size: 1.1rem;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 600;
            box-shadow: 0 4px 16px rgba(245,109,68,0.13);
            cursor: pointer;
            transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
            outline: none;
            text-decoration: none;
            display: inline-block;
            text-align: center;
                        width: 70%;
        }
        .btn-fofo:hover, .btn-fofo:focus {
            background: #ff7d4d;

            transform: translateY(-2px) scale(1.04);
            box-shadow: 0 8px 24px rgba(245,109,68,0.18);
        }
        .log{
            background-color: var(--azulsec);
        }.log:hover, .log:focus {
            background-color: #5faeea;
        }

        @media (max-width: 600px) {
            .msgDev {
                font-size: 1.05rem;
            }
            .dev-img {
                width: 180px;
            }
            .btn-container {
                flex-direction: column;
                align-items: center;
            }
            .btn-fofo {
                width: 90vw;
                padding: 12px 0;
                font-size: 1rem;
            }

            .log{
                background-color: #4a90e2;
            }
        }
    </style>
</head>
<body>
    <section class="dev-container">
        <h2 class="msgDev">Para fazer isso, você precisa fazer login.</h2>
        <img src="/imagem/login-amico-req.svg" alt="Login necessário" class="dev-img">
        <section class="btn-container">
            <a href="/login" class="btn-fofo log">Login</a>
            <a href="/cadastro-usuario" class="btn-fofo cdu">Cadastro de Usuário</a>
        </section>
    </section>
</body>
</html>
